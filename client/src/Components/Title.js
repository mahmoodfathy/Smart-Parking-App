const Title = ({ title, subtitle }) => {
  return (
    <div className="text-center text-3xl mt-3 font-bold">
      <h1>{title} </h1>
      <h1>{subtitle} </h1>
    </div>
  );
};
export default Title;
